if(CONFIG_LIBJANSSON)
  add_subdirectory(.. build)
  set_property(GLOBAL APPEND PROPERTY ZEPHYR_INTERFACE_LIBS libjansson2)
  #zephyr_include_directories(../include)
  zephyr_interface_library_named(libjansson2)
endif()